# Example environment variables for local development
# Client (public) Firebase config - prefix with NEXT_PUBLIC_
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# Server (secret) - Firebase Admin service account JSON (set the full JSON string)
# In PowerShell you can set it temporarily with:
# $env:FIREBASE_SERVICE_ACCOUNT_KEY = Get-Content -Raw 'C:\path\to\service-account.json'
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account", ...}

# Optional: Square keys (if you integrate payments server-side)
# SQUARE_ACCESS_TOKEN=your_square_secret_token
# SQUARE_LOCATION_ID=your_square_location_id
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=
